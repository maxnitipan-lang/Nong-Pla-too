// ตั้งค่า URL ของ backend API ตรงนี้ที่เดียว
// ตอน dev ในเครื่อง: http://localhost:4000
// ตอน deploy จริง: เปลี่ยนเป็น URL ของ backend ที่ deploy ไว้ เช่น https://your-api.onrender.com
const API_BASE_URL = "http://localhost:4000/api";
